<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="LundUniversity.MauiExample.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    FlyoutBehavior="Disabled"
    Title="University Demo">

    <!-- This ShellContent is the default page shown when the app starts (route: "main").
         The MainPage instance is provided via DI in AppShell.xaml.cs.
         Additional detail/edit routes are also registered in AppShell.xaml.cs. -->
    <ShellContent
        x:Name="MainShellContent"
        Title="Departments"
        Route="main" />

</Shell>
